import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const secondaryBackground = Color(0xFF191919);
  static const primaryBackground = Color(0xFF070707);
  static const accentBackground = Color(0xFF467DE9);
  static const tertiaryBackground = Color(0xFF222222);

  // Text colors
  static const accentText = Color(0xFFE5E5E5);
  static const primaryText = Color(0xFFE7E7E7);
  static const secondaryText = Color(0xFF707070);

  // Other colors
  static const dividedColor = Color(0xFFFFFFFF);
  static const successColor = Color(0xFF32B332);

  // Opacity colors
  static const backgroundBlur = Color(0xFF070707); // with 70% opacity
  static const dividedColorDark = Color(0xFF000000); // with 8% opacity
}
